@model IEnumerable<RESERVATION.Models.T_COURSEM>

@{
    ViewData["Title"] = "Index";
}

<h1>時間</h1>

<p>
    <a asp-action="Create" class="blinking-button">&#x271A;</a>

</p>
<div class="w-100 overflow-auto">

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.alertMessage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.alertMessage)
            </td>
            <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary">&#x1F4DD;</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-secondary">&#x1F4C3;</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-secondary">&#x1F5D1;</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>